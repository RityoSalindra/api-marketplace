CREATE TABLE IF NOT EXISTS public.pembelis
(
    id integer serial primary key,
    nama_pembeli character varying(35) COLLATE pg_catalog."default",
    no_telp character(15) COLLATE pg_catalog."default",
    alamat character varying(50) COLLATE pg_catalog."default",
    CONSTRAINT pembeli_pkey PRIMARY KEY (id)
)

TABLESPACE pg_default;

ALTER TABLE IF EXISTS public.pembelis
    OWNER to postgres;